# Core dependencies
streamlit
PyPDF2
requests
numpy>=1.22,<2  # ChromaDB 0.4.x uses np.float_ etc., removed in NumPy 2.0

# Optional but recommended
chromadb==0.4.24  # For persistent vector storage
ollama  # For local LLM and embeddings

# Document processing extras (optional)
unstructured[pdf]  # Enhanced PDF processing

# Audio processing dependencies
SpeechRecognition  # For audio transcription
pydub  # For audio format conversion and manipulation
# Note: pydub requires ffmpeg to be installed on your system
# Install ffmpeg: https://ffmpeg.org/download.html

# Video visual vectorization (optional)
opencv-python-headless>=4.5,<4.10  # Frame extraction; 4.10+ needs numpy>=2 which conflicts with chromadb
sentence-transformers  # CLIP embeddings for images and text
Pillow  # Image handling for CLIP

# App config (optional)
python-dotenv  # For .env (CHROMA_PATH, LLM_MODEL, etc.)
